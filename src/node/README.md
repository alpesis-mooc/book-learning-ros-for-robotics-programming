# Chatter

```
$ bash /opt/ros/<distri>/setup.bash
$ roscore
$ ./_build/chatter_publisher
$ ./_build/chatter_subscriber

$ rosnode list
$ rosnode info /chatter_publisher

$ rostopic list
$ rostopic info /message
$ rostopic type /message
$ rostopic bw /message
```
